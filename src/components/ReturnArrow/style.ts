import styled from 'styled-components';

export const ReturnArrowStyle = styled.aside`
  width: 100%;
  padding-left: 1rem;

  img {
    
  }

  :hover {
    color: #D0E10C;
    cursor: pointer;
  }
`