import styled from 'styled-components';

export const ReturnArrowStyle = styled.aside`
  width: 100%;
  padding-left: 5vw;

  img {
    width: auto;
    height: 2rem;
  }

  :hover {
    color: #D0E10C;
    cursor: pointer;
  }
`